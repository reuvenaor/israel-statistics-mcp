---
---

Release bookkeeping only: `server.json` synced to 1.1.0. No package change — `changeset version` bumps `package.json`/CHANGELOG but not the hand-maintained `server.json`, whose version appears in four places including the tag inside the OCI identifier.
