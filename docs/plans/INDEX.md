# Implemented plans

- 2026-08-05 — [Nightly live suite and correctness fixes](2026-08-05-nightly-live-suite-and-correctness-fixes.md) — the 8-night nightly failure was an unpinned MCP Inspector floating to v2 (exit 5 on `isError`), not CBS drift; also fixed silent 100-row pagination truncation, a timezone-dependent housing provisional window, quarterly series reporting "no data", and the workflow bug filing a duplicate issue every night.
- 2026-08-06 — [Clear all dependency advisories + Docker image security review](2026-08-06-clear-all-dependency-advisories.md) — 12 advisories to zero (only `@hono/node-server` needed a real upgrade; SDK 1.30 widened the range), `pnpm.overrides` floors documented with GHSA ids, and an image-level review that confirmed the npm layer clean, accepted the unfixable Debian `perl` CVEs with evidence, and caught a second Inspector v2 regression that would have turned CI's docker job red.
